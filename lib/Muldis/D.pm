use 5.006;
use strict;
use warnings;
package Muldis::D;
our $VERSION = '0.200000';
$VERSION = eval $VERSION;
# Note that this Perl code file only exists at all in order to help
# the CPAN indexer handle the distribution properly.
1;
